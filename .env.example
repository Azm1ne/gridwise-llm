# TEMPLATE ONLY — this file is committed to git.
# NEVER put a real key here. Copy it to the git-ignored env file and fill that in.
#
#   cp .env.example .env    then edit that copy
#
LLM_PROVIDER=gemini
LLM_MODEL=gemini-3.5-flash-lite
# Any one of these names is accepted: LLM_API_KEY, GEMINI_API_KEY, GOOGLE_API_KEY
GEMINI_API_KEY=
